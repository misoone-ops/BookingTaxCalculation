
# Assets

- `samples/` — metti qui file **mock/anonymized** per test (PDF). C'è un mock di testo per testare le regex.
- `dropbox/` — cartella **watched**: con `tools/watch_drop_folder.py` ogni PDF droppato viene analizzato e salvato `.analysis.json` + `.netto.txt` accanto.

> Non committare dati personali. Usa mock o dati offuscati.
