
# Booking Netto — Backend Skeleton

Vedi README separato per dettagli completi. Questo repo include:
- FastAPI `/analyze`
- Estrattori: invoice2data + fallback pdfplumber
- Regole in YAML
- Script CLI per analizzare una cartella
- Watcher per `assets/dropbox` (autoprocess)
- Struttura `assets/` per sample e drop
